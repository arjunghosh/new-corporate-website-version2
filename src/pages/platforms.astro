---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { site } from '@/data/site';
import { loadLegacyPage } from '@/lib/legacy-page';

const page = await loadLegacyPage('platforms.html');
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonical={`${site.url}/platforms`}
  ogImage={`${site.url}/og/platforms.png`}
  inlineStyles={page.inlineStyles}
  schemas={page.schemas}
  activePath="/platforms"
>
  <Fragment set:html={page.mainHtml} />
</BaseLayout>
