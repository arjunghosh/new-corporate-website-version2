---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { site } from '@/data/site';
import { loadLegacyPage } from '@/lib/legacy-page';

const page = await loadLegacyPage('solutions.html');
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonical={`${site.url}/solutions`}
  ogImage={`${site.url}/og/solutions.png`}
  inlineStyles={page.inlineStyles}
  schemas={page.schemas}
  activePath="/solutions"
>
  <Fragment set:html={page.mainHtml} />
</BaseLayout>
