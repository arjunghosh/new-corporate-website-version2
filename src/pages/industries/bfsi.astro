---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { site } from '@/data/site';
import { loadLegacyPage } from '@/lib/legacy-page';

const page = await loadLegacyPage('bfsi.html');
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonical={`${site.url}/industries/bfsi`}
  ogImage={`${site.url}/og/bfsi.png`}
  inlineStyles={page.inlineStyles}
  schemas={page.schemas}
>
  <Fragment set:html={page.mainHtml} />
</BaseLayout>
