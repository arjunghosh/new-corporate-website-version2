---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { site } from '@/data/site';
import { loadLegacyPage } from '@/lib/legacy-page';

const page = await loadLegacyPage('solutions-ai-ml-engineering.html');
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonical={`${site.url}/solutions-ai-ml-engineering`}
  ogImage={`${site.url}/og/solutions-ai.png`}
  inlineStyles={page.inlineStyles}
  schemas={page.schemas}
  activePath="/solutions"
>
  <Fragment set:html={page.mainHtml} />
</BaseLayout>
