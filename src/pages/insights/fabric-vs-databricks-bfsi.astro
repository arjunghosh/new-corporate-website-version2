---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { site } from '@/data/site';
import { loadLegacyPage } from '@/lib/legacy-page';

const page = await loadLegacyPage('insights-fabric-vs-databricks-bfsi.html');
---

<BaseLayout
  title={page.title}
  description={page.description}
  canonical={`${site.url}/insights/fabric-vs-databricks-bfsi`}
  ogImage={`${site.url}/og/insight-fabric-vs-dbx.png`}
  ogType="article"
  inlineStyles={page.inlineStyles}
  schemas={page.schemas}
  activePath="/insights"
>
  <Fragment set:html={page.mainHtml} />
</BaseLayout>
